---
entry_id: 20260908_204810_hobkg_009_rev
timestamp: 2026-09-08T20:48:10.152356-04:00
project: hobkg
task_id: 009
task_title: Representation-agnostic estimator, fit on development data only
mode: review
model: opus
reviewer_model: opus
status_before: REVIEW
status_after: DONE
git_sha_project: aa7a8dfeb2668fb42f1aa254cda404e25c7d209f
git_sha_orchestrator: 26eb89204893b195e08362da511f53689cabc659
python_version: 3.14.0
lockfile_hash: 1132929d0ced89d89902f37fd31ccd263a5ac477b459550cdc33f6bf16758a12
duration_seconds: 45.73527419997845
verdict: PASS
output_file: audit/executor_output/009_rev.output.txt
---
# Audit Entry: 20260908_204810_hobkg_009_rev

## Notes

VERDICT: PASS

CRITERIA:
- **Single entry point accepts arrays + frozen split, nothing that identifies the representation as a fitting input** — PASS. `fit_and_predict(features, outcome, *, groups, obs_ids, objective, model_id, target, representation, weights, seed, split_parquet, split_manifest, runs_dir)`. `model_id/target/representation` are present but the docstring and code confirm they are labels only: "handed to no function that touches the matrix. The core search-and-fit routine receives only arrays, an objective, the frozen folds and a seed." Card notes explicitly authorize the representation *name* as a run-record label (criterion 11 requires it), so this is consistent, and enforcement is deferred to the provenance-blindness test below.
- **Real vs random matrix through the identical call** — PASS. `test_real_and_random_matrix_complete_through_identical_call` passes `wired.features` and `rng.normal(size=wired.features.shape)` through the same `fit_and_predict`, asserting both complete with a prediction per dev row and no NaNs.
- **Opaque array; never inspects column names, never imports `deckbench.identity`, never reads a feature parquet** — PASS. `test_module_does_not_import_the_representation_builders` asserts `"deckbench.identity" not in source` and `"read_table" not in source`. The only parquet read is the split via `load_dev`.
- **Both objectives supported, caller-selected** — PASS. `BINARY`/`REGRESSION` map to `binary:logistic`/`reg:squarederror`; `test_binary_and_regression_objectives_both_fit` and `test_unknown_objective_is_rejected` confirm selection and rejection.
- **One grid, declared once as data; test asserts a single grid object** — PASS. `HYPERPARAMETER_GRID` is a module constant; `test_single_grid_object_is_used` asserts `seen[0] is seen[1] is estimator.HYPERPARAMETER_GRID` (identity, not equality).
- **Inner CV uses frozen-split folds, never invented; test asserts equality to `model_split.parquet`** — PASS. `_aligned_folds` reads via `dev_fold_map`; `test_fold_assignment_equals_the_split_parquet` compares the used fold vector to the parquet's dev rows.
- **No `draft_id` in more than one inner fold, inherited not re-derived** — PASS. `_aligned_folds` raises `GroupMismatch` if a draft maps to two folds; `test_no_draft_spans_two_inner_folds` asserts one fold per draft.
- **Reads via `load_dev`, never `load_holdout`; source contains no reference** — PASS. `from deckbench.holdout import load_dev`; `test_module_source_never_references_the_sealed_reader` asserts `"load_holdout" not in source`.
- **Ledger byte-identical before/after a full fit** — PASS. `test_holdout_ledger_is_byte_identical_across_a_full_fit` writes a ledger, runs a binary and a regression fit, and asserts `ledger.read_bytes() == before`.
- **Determinism; seed declared and recorded** — PASS. Single-threaded (`_NTHREAD = 1`), seed threaded through; `test_two_fits_same_seed_byte_identical_predictions` asserts array and parquet-byte equality; `DEFAULT_SEED = 20260908` recorded in every record.
- **Run record carries model id, target, representation, feature count, split SHA256, seed, chosen hyperparameters, fitted iteration count** — PASS. `run_record` dict contains all fields; `test_run_record_has_every_required_field` verifies, and asserts `chosen_hyperparameters` is a single grid point (not the whole grid).
- **Split SHA256 read from manifest; fit fails on mismatch** — PASS. `verify_split_hash` reads `split_sha256` and compares to on-disk parquet; `test_fit_fails_when_split_hash_does_not_match` raises `SplitHashMismatch`.
- **Predictions dev-only, keyed by `obs_id`, disjoint from holdout** — PASS. `test_emitted_ids_are_disjoint_from_holdout` checks emitted ids and the parquet keys are disjoint from the holdout partition; `test_a_holdout_obs_id_is_rejected` confirms a holdout id raises `HoldoutRowRejected`.
- **No metric/score/calibration/comparison computed** — PASS. The native `eval_metric` is used only inside `xgb.cv` for grid selection and is never emitted; `test_no_metric_leaks_into_the_run_record` asserts record keys are disjoint from `{r2, rmse, brier, logloss, auc, score, calibration, ...}`.
- **All new tests pass; clean under ruff and mypy --strict** — PASS. Audit entry: `pytest tests/test_deckbench_estimator.py -q` → 20 passed; `ruff check` → All checks passed; `mypy --strict` → Success; `--selftest` exit 0; full `pytest -q` → 647 passed.

SUMMARY:
The estimator is a single, representation-blind code path: it accepts opaque arrays plus the frozen split, carries `model_id/target/representation` only as run-record labels never seen by the fitting routine, searches one module-level grid, draws folds exclusively from `model_split.parquet` via `load_dev`, verifies the split hash against the manifest, fits deterministically single-threaded, and emits development-only out-of-fold predictions with a reproducible run record. It computes and emits no evaluation metric, never references `load_holdout`, and leaves the ledger untouched. Every acceptance criterion is backed by a concrete test or structural property, and the latest passing audit confirms all checks, output validation, ruff, and mypy --strict pass. PASS.
