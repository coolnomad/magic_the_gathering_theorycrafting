---
entry_id: 20260909_141910_hobkg_013_rev
timestamp: 2026-09-09T14:19:10.946416-04:00
project: hobkg
task_id: 013
task_title: Establish which derived artifacts are actually deterministic
mode: review
model: opus
reviewer_model: opus
status_before: REVIEW
status_after: DONE
git_sha_project: 385bc8e9286ed2eaec14b2830d54cc9ddcf164ba
git_sha_orchestrator: 26eb89204893b195e08362da511f53689cabc659
python_version: 3.14.0
lockfile_hash: 1132929d0ced89d89902f37fd31ccd263a5ac477b459550cdc33f6bf16758a12
duration_seconds: 79.88659839995671
verdict: PASS
output_file: audit/executor_output/013_rev.output.txt
---
# Audit Entry: 20260909_141910_hobkg_013_rev

## Notes

VERDICT: PASS

CRITERIA:
- **Harness regenerates each artifact ≥2× in a fresh process, byte-compares, reports stable/unstable/not_regenerable** — MET. `run_child` spawns `subprocess.run([interp, "-c", code], env=_child_env(seed), ...)`; `_child_env` sets a distinct `PYTHONHASHSEED` per seed; default `runs=3` with a `max(2, args.runs)` floor. Verdicts are exactly `stable | unstable | not_regenerable | verified_manifest`. Audit: `69 regenerated, 0 unstable, seeds=[0, 1, 2]`.
- **Compares code-written bytes, not git's view** — MET. Reads via `path.read_bytes()` (binary); `snapshot()`/`restore()` operate on the working tree; nothing compares against a git blob. NARRATIVE explicitly documents the `* text=auto` CRLF/LF one-byte-per-line trap and rejects it.
- **Per-unstable characterization: record count, multiset identical (pure reordering), internal record diff, first differing byte offset** — MET (capability + tests). `characterize()` returns `record_count`, `byte_length`, `multiset_of_records_identical`, `pure_reordering`, `a_record_differs_internally`, `first_differing_byte_offset`; verified by `test_characterize_pure_reordering` and `test_characterize_internal_record_diff`.
- **`reports/determinism_audit.md` lists every derived artifact with verdict + characterization for unstable** — MET. Four tables (regenerated, frozen, deckbench, not-regenerable) enumerate every artifact; `_fmt_char` renders characterization for unstable rows.
- **`llm_accepted.jsonl`/`llm_queued.jsonl` covered; instability reproduced (confirmed case); fix makes two serial regens byte-identical** — MET. Report records the pre-fix measurement (equal length, differing multiset; first-diff offsets 178 and 1247) satisfying "measure before fixing"; `test_reconcile_output_is_byte_identical_across_hash_seeds` proves post-fix byte-identity across seeds 0 and 91237.
- **`data/review` fix is intra-record, not line ordering** — MET. Fix lives in `phase3.reconcile`, sorting the set-derived keys (`c_edges.keys() & k_edges.keys()`, etc.) with total keys before building each record's `disputed_edges`/`disputed_abilities` lists — inside the record, not a file-level sort.
- **`card_pair_projection_audit_repair.jsonl` covered** — MET. Present in the report under job `audit_repair` (stable, 3 runs) and asserted by `test_audit_repair_projection_covered_and_stable`. The card's "no sort / stable by luck" premise was corrected with evidence: `_write(...)` sorts by `(source_card, target_card, relation)` and `add_pair` dedupes on that same triple (total key).
- **Every unstable artifact fixed to agree or documented** — MET. Only the two review files were unstable; both fixed. Summary: "All regenerated artifacts are byte-stable across the seeds tried."
- **Fix changes only ordering; test asserts record multiset unchanged from committed** — MET. `test_fix_preserves_record_multiset_of_committed_review_files` compares canonical (order-insensitive) multisets of the regenerated files against the committed copies for both `REVIEW_FILES`.
- **`frozen_manifest.json` still verifies; frozen base byte-identical** — MET. `_verify_frozen` recomputes sha256 vs pinned; report shows all frozen files "byte-identical to frozen_manifest.json"; `test_frozen_base_verified_byte_identical`; `ports --validate` returned 210 ports/2242 edges clean.
- **Single command, non-zero exit on instability** — MET. `main()` returns `1 if unstable else 0`; `test_main_exit_nonzero_when_an_artifact_is_unstable` covers both branches; `python tools/determinism_audit.py --report` exited 0.
- **All tests pass; no new ruff violation in touched files; mypy clean** — MET. `pytest -q` 715 passed; `test_determinism_audit.py` 10 passed; ruff (E,F,I,B,UP) clean on both new files; `mypy --strict tools/determinism_audit.py` clean. The only modified production file is `phase3.py` (audit_repair.py "needed no change" per report); the added `_ab_order`/`reconcile` lines are all under the 100-char limit and introduce no new E/F/I/B/UP violation.

SUMMARY:
The harness satisfies every acceptance criterion: it regenerates derived artifacts in fresh per-seed subprocesses, compares the bytes the code actually writes (never git), characterizes any instability, and exits non-zero on failure. The one confirmed instability (`data/review/llm_accepted.jsonl`/`llm_queued.jsonl`) was measured before fixing, traced to per-process `set` iteration in `phase3.reconcile`, and fixed with total-order sort keys applied intra-record; a multiset test proves no records changed. The card's earlier guesses (audit_repair "stable by luck", completeness "unstable") were corrected with byte-level evidence rather than repeated. Frozen base and modeling-arm artifacts are verified against their manifests and left untouched. All checks and output-validation commands pass.
