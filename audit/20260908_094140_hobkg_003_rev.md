---
entry_id: 20260908_094140_hobkg_003_rev
timestamp: 2026-09-08T09:41:40.732274-04:00
project: hobkg
task_id: 003
task_title: Audit the raw HOB game-level dataset and fix the observational unit
mode: review
model: opus
reviewer_model: opus
status_before: REVIEW
status_after: DONE
git_sha_project: f331b5599aadf454e44e8160733285e1ba57fa7e
git_sha_orchestrator: dfc6bf76e95917801b5bc993c966f9ada7244d39
python_version: 3.14.0
lockfile_hash: 1132929d0ced89d89902f37fd31ccd263a5ac477b459550cdc33f6bf16758a12
duration_seconds: 65.39152319999994
verdict: PASS
output_file: audit/executor_output/003_rev.output.txt
---
# Audit Entry: 20260908_094140_hobkg_003_rev

## Notes

VERDICT: PASS

CRITERIA:
- **Names each key column verbatim in the .md** — PASS. Section 1 table gives `draft_id` (draft identifier), `won` (per-game outcome), `deck_ column family` (deck contents), `draft_time` + `game_time` (timestamps), `rank`, `user_game_win_rate_bucket` (historical win-rate), `user_n_games_bucket` (games-played). All present.
- **Explicit persistent-player-id statement, no substitute nominated** — PASS. Section 2 (and JSON `player_identifier.statement`): "No persistent player identifier exists in the dataset. rank and opp_rank are six-level skill buckets, not player ids, and are not nominated as a substitute." Uses the required "none exists" framing and refuses `rank`/`opp_rank` as substitutes.
- **Total games/drafts/columns computed, not transcribed** — PASS. Section 3: 241727 games, 43161 drafts, 985 columns. `run_audit` streams and counts; the anti-hardcoding grep test (below) proves these are not source literals.
- **Within-draft deck variation quantified** — PASS. Section 5: distinct-configs-per-draft distribution `1:34970, 2:6636, 3:1314, 4:210, 5:29, 6:2`; drafts with >1 deck = 8191 (0.189778); games after a deck change = 28397 (0.117475). All three required quantities present.
- **Deck-size distribution + illegal flag** — PASS. Section 7 gives the full size→rows distribution (40..60), floor `LEGAL_MIN_DECK_SIZE=40`, and 0 rows below. The module correctly notes Limited has no maximum, so "outside range" = below 40 (per comprehensive rule 100.2b) — a sound rules interpretation, not an omission.
- **Card-count consistency check with a count** — PASS (with disclosed interpretation). Section 8 states no standalone deck-size column exists, defines deck size as the sum of `deck_` columns, and reports rows off the modal legal size = 15680 and rows below legal min = 0. The interpretation is explicitly disclosed rather than silently assumed, which the reviewer rules acceptable given the absent column.
- **Skill-variable within-draft constancy count** — PASS. Section 9: `rank` non-constant in 5420 drafts, `user_game_win_rate_bucket` 0, `user_n_games_bucket` 0.
- **Observational-unit recommendation with justifying numbers** — PASS. Section 10 recommends the game level, citing 0.1898 of drafts with >1 deck and 0.1175 of games after a change, keyed by `draft_id + game_time`, and flags it as a recommendation the operator ratifies at card 007.
- **JSON carries every number machine-readably** — PASS. `reports/modeling_data_audit.json` mirrors all report fields (dataset totals, census, within_draft, deck_size, build_index_behavior, recommendation) for card 004 to consume.
- **Byte-identical JSON across two runs** — PASS. `write_reports` uses `json.dumps(sort_keys=True)` and `_round_floats(..., 6)`; digest is `blake2b` (unseeded). `test_json_report_is_byte_identical_across_two_runs` passed in the audit entry (10 passed).
- **No hardcoded column list / row / draft literals; grep test present** — PASS. Column families are matched by prefix and non-card columns computed; `test_module_source_has_no_hardcoded_totals` greps `audit.py` and asserts `total_games`/`total_drafts`/`total_columns` do not appear as literals. Verified absent in source (semantic anchors like `"draft_id"` are individual role constants, not the file's column list).
- **Chunked read, no full 1165-column matrix in memory** — PASS. `_iter_chunks` batches the `csv.reader` at `CHUNK_ROWS=20000`; only per-draft aggregates (digests, skill values) accumulate — no full matrix retained. Observed 985 columns (≠ declared 1165) is surfaced as a discrepancy, per the card's instruction to report rather than reconcile.
- **Tests pass; clean under ruff and mypy --strict** — PASS. Audit entry: `pytest tests/test_deckbench_audit.py -q` → 10 passed (both check and validation); `ruff check --select E,F,I,B,UP` → all passed; `mypy --strict src/deckbench/audit.py` → success.

SUMMARY:
Every acceptance criterion is satisfied with concrete evidence from the delivered files and the passing audit entry. The audit computes all totals by streaming the raw CSV, discovers column families by prefix rather than a baked-in list, names each semantic column verbatim, refuses to nominate a player-id substitute, quantifies within-draft deck variation and build_index behavior, and recommends the game-level unit with the measured fractions that justify it. Determinism, anti-hardcoding, chunked reading, and lint/type cleanliness are all independently verified by passing tests and validation commands. Two judgment calls — defining the card-count check against the modal legal size (no standalone deck-size column) and treating "illegal" as below the 40-card floor (Limited has no maximum) — are explicitly disclosed in the report and are sound, and the 985-vs-1165 column mismatch is correctly reported as a finding.
